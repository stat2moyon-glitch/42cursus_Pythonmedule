### defがいらない理由

Pythonは
ファイルそのもの
↓
上から処理を実行できる

ゆえに
def は「Pythonを書くために必要」なのではない。

「関数を作りたいとき」に使う。

ーー詳細
Pythonはファイルの一番外側に処理を書けば、そのまま上から実行される。
だから課題が単に、「この表示をしなさい」なら、関数に包む必要はない。

ただし、ここで def を使った場合は、「処理を実行する」のではなく「処理を関数として登録する」だけになる。

「garden っていう関数ね。中身はこれね。覚えとくわ」
っで終わり。

実行したければ、
```
def garden() -> None:
    print("Hello")

garden()
```
と書かないといけない。

###　シェバンが要る理由

パイソンをシェルで実行しようと思ったら

python3 test.py

と書かないといけない。
でも、ファイル側にシェバンを書いておくと

chmod +x test.py
./test.py

で実行できる。

### .

import test
test.add()
と、
from test import add
add()
は同じ。

